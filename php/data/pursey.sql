CREATE DATABASE `pursey` DEFAULT CHARACTER SET utf8;

USE pursey;

CREATE TABLE tb_advertising
(
	id INT(4) UNSIGNED NOT NULL AUTO_INCREMENT,
	title VARCHAR(100) NOT NULL,
	content VARCHAR(500) NOT NULL,
	fdate DATE NOT NULL,
	flag INT(1) NOT NULL DEFAULT 0,
	PRIMARY KEY(id)
);

CREATE TABLE tb_admin
(
	id INT(4) NOT NULL AUTO_INCREMENT,
	name VARCHAR(50),
	pwd VARCHAR(50),
	PRIMARY KEY(id)
);

CREATE TABLE tb_info
(
	id INT(4) NOT NULL AUTO_INCREMENT,
	type VARCHAR(30) NOT NULL,
	title VARCHAR(50) NOT NULL,
	content VARCHAR(500) NOT NULL,
	linkman VARCHAR(20) NOT NULL,
	tel VARCHAR(30) NOT NULL,
	checkstate INT(1) NOT NULL DEFAULT 0,
	edate DATE NOT NULL,
	PRIMARY KEY(id)
);

CREATE TABLE tb_leaguerinfo
(
	id INT(4) NOT NULL AUTO_INCREMENT,
	type VARCHAR(20) NOT NULL,
	title VARCHAR(50) NOT NULL,
	content VARCHAR(500) NOT NULL,
	linkman VARCHAR(20) NOT NULL,
	tel VARCHAR(30) NOT NULL,
	sdate DATE NOT NULL,
	showday DATE NOT NULL,
	checkstate INT(1) NOT NULL DEFAULT 0,
	PRIMARY KEY(id)
);

GRANT SELECT, INSERT, UPDATE, DELETE
ON pursey.*
TO pursey@localhost IDENTIFIED BY 'password';
